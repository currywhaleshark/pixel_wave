# fonts

픽셀 폰트 파일을 여기에 둡니다.

```
Galmuri9.woff2   (+ .ttf)
Galmuri11.woff2  (+ .ttf)
```

- 규격·후보·라이선스: [`docs/ART_SPEC.md`](../../docs/ART_SPEC.md) 7장
- 코드 쪽 정의: [`js/fonts.js`](../../js/fonts.js) · `index.html`의 `@font-face`

파일이 없으면 자동으로 sans-serif로 폴백합니다 — 게임은 그대로 동작합니다.
