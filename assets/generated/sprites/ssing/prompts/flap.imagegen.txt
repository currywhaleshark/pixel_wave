Create a single horizontal sprite strip for game boss `ssing`, state `flap`. Image 1 is the accepted canonical native 56x40 pixel-art identity anchor; preserve its exact left-facing proportions, determined single eye and brow, dark navy center body, broad periwinkle wings, pale lavender belly markings, short tail, attached red scarf knot, palette, outline weight, and coarse logical pixel density. Image 2 is layout-only guidance; do not reproduce any guide marks.

Exactly 2 complete full-body poses left to right, evenly centered in two invisible equal 56x40 slots with at least 2 logical pixels margin. Frame 1 has both broad wings moderately raised. Frame 2 presses both wings lower by 3-4 logical pixels. Keep the eye, brow, center body, scarf knot, tail root, horizontal body center, total body length, scale, and palette fixed. The two frames must form a seamless 6fps alternating flap loop.

Use true hard-edged native-resolution coarse pixel art with countable square logical pixels, one-pixel dark indigo outline, maximum 14 flat navy/periwinkle/lavender/red colors, no antialiasing, gradients, blur, or microtexture. Perfectly flat solid #00FF00 green chroma-key background edge-to-edge. Exactly one manta per slot, no long scarf ribbon because runtime draws it, no detached parts, no shadows, glow, water, bubbles, scenery, speed lines, text, UI, boxes, grids, labels, or watermark. No pose may overlap or cross slots. Output only the two-frame sprite strip.
