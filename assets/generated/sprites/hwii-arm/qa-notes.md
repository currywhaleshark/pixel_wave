# Hwii arm QA

- `flow`: **pass** — the broad left mounting root stays fixed while the middle arc and distal hook form two distinct whole-arm wave poses. The two-frame loop reads as a controlled pulse with no hard translation, detached cloud pieces, silhouette break, or palette flicker.
- Runtime intent: draw one frame behind the Hwii core, reuse it three times at 120-degree offsets, and keep the procedural line arms only as a loading fallback.
