Correct the two-frame raw row so both poses use exactly the same square logical pixel block size, bell width and height, outline thickness, character height, top alignment and bottom baseline. Keep the left design unchanged. The right pose may differ only by moving the three tentacle tips and attached lantern strand by one logical block and compressing the lower bell edge by one logical block. Preserve flat #00FF00 outside the sprites; no shrinking, finer pixels, antialiasing, detached pixels, glow, guide boxes, text or extra poses.
