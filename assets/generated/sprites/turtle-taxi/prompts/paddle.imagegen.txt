Create a single horizontal sprite strip for game character `turtle-taxi`, state `paddle`. Image 1 is the accepted canonical native 32x20 pixel-art identity anchor; preserve its exact proportions, right-facing orientation, shell shape, four attached flippers, tiny eye, navy driver cap, palette, outline weight, and coarse logical pixel density. Image 2 is layout-only guidance; do not reproduce any guide marks.

Exactly 2 complete full-body poses left to right, evenly centered in two invisible equal 32x20 slots with at least 1 logical pixel margin. Frame 1 matches the neutral paddle pose. Frame 2 alternates the four attached flippers by only 1-2 logical pixels while the head, eye, cap, shell platform, body center, footprint, scale, and baseline remain fixed. The two frames must form a seamless 6fps alternating swim loop.

Use true hard-edged native-resolution coarse pixel art with countable square logical pixels, one-pixel dark teal outline, maximum 12 flat olive/mint/navy colors, no antialiasing, gradients, blur, or microtexture. Perfectly flat solid #FF00FF magenta chroma-key background edge-to-edge. Exactly one turtle per slot, no passenger, no detached parts, no shadows, glow, bubbles, water, scenery, speed lines, text, UI, boxes, grids, labels, or watermark. No pose may overlap or cross slots. Output only the two-frame sprite strip.
